# Copy this to .env.local (gitignored) and fill in your values.
# VITE_ prefix is required for Vite to expose variables to the browser.

# Must match SECRET_KEY in backend/.env
# Leave empty in dev (auth is skipped when SECRET_KEY is not set on the backend)
VITE_API_KEY=
